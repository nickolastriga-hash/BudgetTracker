rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cloud backup: one document per user, readable/writable only by them.
    match /backups/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
